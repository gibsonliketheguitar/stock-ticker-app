{
  "private": true,
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "start": "set NODE_ENV=production&& node server.js",
    "dev": "set NODE_ENV=development&& nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "restart": "npx kill-port 3001 && yarn run dev"
  },
  "dependencies": {
    "body-parser": "^1.20.1",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "finnhub": "^1.2.16",
    "helmet": "^6.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
